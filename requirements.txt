# Core ML
torch>=2.0.0
transformers>=4.30.0
faiss-cpu>=1.7.4

# Image processing
Pillow>=10.0.0
albumentations>=1.3.0
opencv-python-headless>=4.8.0

# OCR & text matching
easyocr>=1.7.0
pytesseract>=0.3.10  # Fast number OCR (requires Tesseract binary installed)
rapidfuzz>=3.0.0

# Data
numpy>=1.24.0
tqdm>=4.65.0
requests>=2.31.0

# API
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Card detection (inference)
onnxruntime>=1.15.0
python-doctr[torch]>=0.9.0  # DocTR text-based card detector (fallback)

# YOLO card detection (training only — optional, not needed for inference)
# ultralytics>=8.1.0

# Gemini AI (alternative recognition & grading)
google-genai>=1.0.0

# Claude grading (pregrading /grade endpoint) — pin exact: uses newer SDK params
# (output_config json_schema, thinking=adaptive, effort) that require this floor.
anthropic==0.111.0

# Dev / Testing
pytest>=7.0.0
httpx>=0.24.0
